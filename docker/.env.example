# docker/.env.example
# Production environment variables for Kiwari POS

# ── PostgreSQL Configuration ──────────────────────────
# For production: use 'pos-db' as hostname (Docker service name)
# For local development: use 'localhost' if connecting from host machine
POSTGRES_USER=pos
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=pos_db

# Database connection URL for Go API
# Production (inside Docker network): postgres://pos:password@pos-db:5432/pos_db?sslmode=disable
# Development (from host): postgres://pos:password@localhost:5432/pos_db?sslmode=disable
DATABASE_URL=postgres://pos:change-me-in-production@pos-db:5432/pos_db?sslmode=disable

# ── API Configuration ─────────────────────────────────
# Server port (internal to container, mapped to host via docker-compose.yml)
PORT=8081

# JWT secret for authentication tokens (generate a strong random string)
JWT_SECRET=change-me-in-production-use-long-random-string

# ── Optional: Environment ─────────────────────────────
# Environment mode (development, staging, production)
# APP_ENV=production
